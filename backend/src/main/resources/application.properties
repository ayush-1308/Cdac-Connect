server.port=8080

# === Datasource Configuration ===
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/firstdb
spring.datasource.username=root
spring.datasource.password=Ronak
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === JPA / Hibernate ===
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.format_sql=true
# === H2 Console ===
#spring.h2.console.enabled=true

# === JWT Configuration ===
jwt.secret=FGW390@#7&Ounjkebu!#%%GHOPQasx&Q
jwt.expiration=3600000


spring.security.user.name=admin
spring.security.user.password=admin123


# ===================================================================
# JPA AND HIBERNATE SETTINGS
# ===================================================================
# This is the most critical fix for your errors.
# It tells Hibernate to generate SQL syntax that is compatible with MySQL (using AUTO_INCREMENT).
# This resolves both the "generated by default as identity" and "Unknown table 'SEQUENCES'" errors.

# ===================================================================
# This is highly recommended for development. It will print the actual SQL
# queries generated by Hibernate to the console, helping you debug issues.


# This formats the SQL printed by 'show-sql' to be more readable.
